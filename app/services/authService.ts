import { ICredential } from "@/models/interfaces/ICredential"

export const signInService = async (userCredential: ICredential) => {
    return await 'something'
}

export const signOutService = async () => {
    return await 'something'
}